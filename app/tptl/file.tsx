export default function Home() {

/* 14 What is the difference between React elements and React components?

first its needed to explain:
React element is a simple object that represents what the UI should look like.
It is immutable
It describes the UI at a specific moment
Created via JSX
example:  */


/*   
  const element = (
      <button className="btn-primary" onClick={() => alert("Clicked!")}>
        Click me
      </button>
     );
 */


/* React component is a function or class that returns React elements.
It is reusable
It can contain logic, state, lifecycle
It defines how UI is built
example: */


/* 
function Greeting({ name }) {
  return <h1>Hello, {name}</h1>;
}

 */










}