To use or not return:


My question:
would you say when i use find with an array function or array with objects, i'll always have to return before like in return menu.find() ?

Answer:

The point is actually not:

"Am I using find()?"

The point is:

"Do I want my function to return the result of find()?"

Case 1: Yes, use return
function getPizzaDetail(id: number) {
    return menu.find(pizza => pizza.id === id)
}

const pizza = getPizzaDetail(2)

getPizzaDetail() is supposed to give the caller a pizza, so you return the result of find().

Case 2: No, don't use return

Suppose you only want to log the pizza:

function printPizza(id: number) {
    const pizza = menu.find(pizza => pizza.id === id)
    console.log(pizza)
}

printPizza(2)

No return is needed because the function's job is to print something, not hand a value back.


BUT THIS IS BECAUSE THE .find DOESNT AUTOMATICALLY RETURN THE VALUE


So with these it wont be necessary to return manually:




----------------------

constructor:

there are THREE types of constructors:

Constructor functions = writing a letter by hand
Classes = typing a letter on a computer

constructor function:

the constructor is a way of creating objects automatically

passing unique data to the constructor to construct these objects

"seems to be a reusable method to define properties and methods of objects we create"


function Car(make, model, year, color) {
	this.make = make,
	this.model = model,
	this.year = year,
	this.color = color
}
^^^^ to use this constructor i'll need to create an instance of an object

this function 'car' is a special method (Special methods in JavaScript are built-in or user-defined functions that trigger automatic behaviors)


or user-defined functions that trigger automatic behaviors


THE ACTUAL IMPORTANT ONE


class constructor:
it's a special syntax — a way of writing code that lets you define a template with properties and behaviors, then stamp out multiple objects that all share the same structure.

when doing: class Dog { ... }

This defines a new type of thing you can create. Dog becomes a special function (a constructor) that you call with new to make individual dog objects.










----




Built in javascript constructors (already provided by JS)


Constructor				What it does

Date					Dates and times
Promise				Async operations
Map					Key-value pairs (any key type)
Set					Unique values
Error, TypeError, ReferenceError	Errors
RegExp	Regular expressions
Array					Arrays (though [] is more common)
Object					Objects (though {} is more common)


Rarely Used (You might never see these)

Constructor				What it does

String, Number, Boolean		Almost always use primitives instead
WeakMap, WeakSet			Specialized memory-leak prevention
Int8Array, Uint16Array, etc.		Binary data (advanced)
ArrayBuffer, SharedArrayBuffer	Low-level binary memory
BigInt64Array, BigUint64Array	Big integers in arrays
DataView				Low-level binary access


Web-Specific (Browser-only)

Constructor				Where it works

URL					Browsers + Node.js
Headers, Request, Response		fetch API
Blob, File				File handling
FormData				Form submission


--


What If You Name Your Constructor the Same as a Built-in?
Bad idea — let me show you why:

javascript
// DON'T DO THIS

function Date() {
    this.custom = true;
}
const myDate = new Date();  // Which Date? Yours or built-in?


"Whenever you name your constructor the same as a built-in, expect the built-in to be OVERWRITTEN in your current scope. This breaks everything that expects the original Date, and other developers will hate debugging this."




How to Avoid This Problem
Use Different Names (Best Practice) **CHECK IF EXISTS A BUILT IN CONSTRUCTOR**
theres still always a chance you'll use a
ORRR just use typescript


"Whenever you try to declare something with a name that already exists in the current scope, TypeScript's default behavior is to treat this as an error. The built-in types are always in scope (via lib.d.ts), so you'll get a 'duplicate identifier' error."


--------------


Promise:

1. What’s inside the Promise constructor (logically speaking)
Technical description:
The Promise constructor takes a single argument called the executor function — a function that the constructor synchronously calls with two parameters: resolve and reject. Inside the executor, you typically start an async task, then call resolve(value) on success or reject(error) on failure.

“Whatever async work you do, itll use resolve to send back the successful result, or reject to send back the error.’ The constructor’s job is to store those resolve/reject controls and later connect them to .then() or .catch().”


2. When you use new Promise(...), what you’re expecting to get
Technical description:
new Promise(executor) returns a new Promise object in the pending state. That object has internal slots ([[PromiseState]], [[PromiseResult]], [[PromiseFulfillReactions]], etc.) and publicly exposes .then(), .catch(), and .finally() methods.

“When you write new Promise(...), you get back a special ‘future ticket’ object. That ticket starts in ‘waiting mode’ (pending). It doesn’t have the final value yet. What you can do right away is hand that ticket to someone else, who can attach reactions using .then() — those reactions will automatically run later when the ticket changes to ‘done’ (fulfilled) or ‘failed’ (rejected).”




--------------




There are many ways the callback present itself

when the callback has empty parentheses ()

	setCount(function() {
                return count + 1                    
    })
What this says:
"I'm telling setCount: 'Here's a function that takes no incoming data from you. Instead, it does the code inside {}. Whats the point?: well using empty () is mostly a habit or a mistake — not a best practice.'"

when the callback is as alone as it can be 

setTimeout(() => {
    console.log("Time's up!")
    document.title = "Done"
}, 1000)
"The syntax above is a alternative to function() callback, its the same code but better"


when the call back has no curly braces {}

playlistArr.map(playlistItems => playlistItems.toUpperCase())
"no curly braces in the syntax means to follow this different one with access operator, and probably others as well"

when the call back is in single parameter

playlistArr.map(playlistItems => {
  return playlistItems.toUpperCase();
})
"when you want to shorten the function(callback) kind of callback or use some detail functionality of =>"

when the call back is in single parameter BUT wraped in ()

playlistArr.map((playlistItems) => {
  return playlistItems.toUpperCase();
})
"In this one theres literally no difference, except that sometimes you may need to add another callback (callback1, callback2) so adding it previously may be "beneficial"   ."




--------------




React.children:

utility that provides methods for interacting with a components direct children elements\

being the methods:
 .map() forEach()..etc                        (React.children.map())     (React.children.forEach())

when using React.children.map()
is different than javascript .map

So the first thing needed to provide is the array to interact with.

Characteristics:
Works only on arrays.
Returns a new array.
Does not modify the original array.
Commonly used to transform data.

Best Practices
Keep utilities pure (no side effects). 
Place them in dedicated files (e.g., utils/, helpers/).
Avoid passing utility functions deeply through props—import them directly.
Use ES6 modules (export/import) for clean sharing across components.


--------------



about function and const

Use const for function expressions by default.  It prevents accidental 
reassignment and aligns with modern ES6+ standards for immutability.
Use function declarations when you need hoisting (e.g., defining utility 
functions that are called before their definition in the file) or when you 
prefer explicit named functions for better stack traces. 
In React, const is predominantly used for functional components to ensure the 
component reference remains stable and to avoid hoisting-related confusion in JSX. 





The primary difference between function declarations and const function expressions 
in JavaScript is hoisting.  Function declarations are hoisted to the top of their scope, 
meaning they can be called before they appear in the code.  In contrast, const function expressions 
(including arrow functions) are not hoisted; attempting to call them before their declaration results in a ReferenceError. 


it seems function is used loads because of this flexibility for some reason, 
it may have deeper meaning the way this works (being able to call before its declaration)





-------------- --------------

Going back on a few resolutions havent touched in a while:

-------------- --------------
Advanced React
Reusability
Challenge - add onClick event listener
Exercise #1


So in this code ive got to remember how calling of a func and using ()
will immediately trigger it even while being inside a onClick for example.

**Actually what ive got was an error, and what the AI said in scrimba wasnt correct**

function App() {
  const clicked = console.log('Logging in')
  return (
    <main>
      <Button onClick={clicked()}>
        <FcGoogle />
        Log in with Google
      </Button>
    </main>
  )
}



-----
The workarround to the error:

wrapping clicked in an event handler callback arrow function

<Button onClick={() => clicked()}>

this fix the error but the console.log still immediately gets called...


Besides that, it does indeed get rid of the error, why?

because that event handler callback arrow function mas the clicked function
wait for the onClick to be activaded.


-----

Ive made a 4 way process of remembering these scenarios, Its on Anki cards.


--------------
Advanced React
Reusability
Button - size prop
Exercise #1


For this one i have but one question

How an IIFE compares to this function

export default function Button({size, children, ...rest}) {
    let sizeSM = {}
    size === 'sm' ? sizeSM = 'button-small' : null
    
    size === 'lg' ? sizeSM = 'button-large' : null
    
    return (
        <button className={sizeSM} {...rest}>
        {children}
        </button>
    )
}


I do know as a fact that react makes the ternary evaluation right
away, and that cus theres that return there the Button function 
will return it and itll be displayed.

But with that, i want to know if it could as well be considered 
something that is being "immediately invoked", i know its not an IIFE!,
but its logic sure seem to be close to what IIFE name is implying 
(immediately invoked function expression)

Definitive Answer: 

The timing is similar in that both run "right away" in their context:

IIFE: runs immediately upon definition

Your ternaries: run immediately when the Button function is called


So yeah i was right.

--------------
Advanced React
Reusability
The React.Children API
Exercise #1


An old thing i'm remembering

export default function Menu({ children }) {

        <div className="menu">
            {React.Children.map(children, (child) => {
                return React.cloneElement(child, {
                    toggle,
                    open
                })
            })}
        </div>
        }


the important part is .map(children, (child) => {...})

I didnt got to go deeper into understanding this, a question i have
is how theres 'children' in there where should be a callback, and after a comma,
the actuall callback (child), how is this working and why the use of children in there,
also it seems that that children is the passed prop to Menu function. So whats going on here. 


Answer:

So actually, React.Children.map() takes two parameters.

1 first - The children to iterate over

2 second - The callback function to apply to each  (just like normaly is with .map)

So the whole code works to allows each MenuItem to receive toggle and open props 
without having to pass them manually.

--------------
