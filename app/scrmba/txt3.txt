Continuation from txt 2 file:

*****objective of file is to get the needed info to finish commerce project*******

--------------------------------

TypeScript Fundamentals - Intro to Pizza app - Exercise #2


    const order = orderQueue.find(
        orderObj => orderObj.id === orderId
    )


In here theres a usage of .find


.find() calls your callback on each item, but it inspects the return value.

If your callback returns true, .find() immediately stops searching and hands you
that item.



**                                  {
    const order = orderQueue.find(
        orderObj => orderObj.id === orderId
    )

    order.status = "completed"

    return order
}**


in that part is .find triggering order.status and return after getting the value true? sorta like a if statement?

yes:

.find() is literally a loop with an if statement inside it.
The callback orderObj => orderObj.id === orderId is just the condition for that if statement.


When that condition becomes true, .find() "triggers" and returns that item.

so its sort of working like a if statement without {}




MIND YOU that the function from the code above (orderId)

was taking a parameter by a calling of the function

and the value was (2) so orderId in there is 2




--------------------------------

TypeScript Fundamentals Defensive coding


remember this code, it maybe useful in the future.



(remember that cashInRegister += selectedPizza.price was showing an
red line error "Object is possibly undefined").



    if (!selectedPizza) {
        console.error(`${pizzaName} does not exist in the menu`)
        return
    }
    cashInRegister += selectedPizza.price



Seems like with this kind of if statement typescript gets satisfied

And that by using 'return' in there it will never reach 'cashInRegister += selectedPizza.price'



ALSO remember this:

        if (!order) {
        return
    }


So how is the teacher saying (by using 'return' in there it will never reach (order in  this case))
actually even work? how is the return preventing from reaching that line?


--------------------------------



TypeScript Fundamentals Defining Custom Types



Custom types


Is basically a way of adding new types to anything,
and is very useful for creating types around objects



why in here:

const menu: Pizza[] = [
    { name: "Margherita", cost: 8 },
    { name: "Pepperoni", cost: 10 },
    { name: "Hawaiian", cost: 10 },
    { name: "Veggie", cost: 9 },
]

i had to use [] on : Pizza[]

while in here not

addNewPizza(pizzaObj: Pizza)


Answer: is because menu is an array.

Pizza[] explicitly tells TypeScript that the variable holds a collection (list) of items.


--------------------------------

Nested object types Exercise #1



Theres a way of using type inside another to
define data:


type Person = {
    name: string
    age: number
    isStudent: boolean
    address: Address                <<< here Address is another declared type
}




--------------------------------

Instead of

: Pizza[]


We can also use

Array<Pizza>


--------------------------------

Literal types



We can use a string as a type:

const myName2: "Bob" = "Bob"


: "Bob"


literal types can be useful but is more commonly found with the concept called "unions"



--------------------------------


Unions


we can use the single pipe to set an type that can be only 3 things or more.


type UserRole = "guest" | "member" | "admin"

let userRole: UserRole = "member"


ALSO unions dont mean just the above, they also mean:


function name(identifier: string | number)



--------------------------------

Update order status to use literal type unions - broad-block




So basically here were getting a typescript red line, but it doesnt specify
whats actually is going on, the only lead i had was that it was a error flag
not on the declared value, but on the CALLING of the value (which was pushed: push(newOrder))


    const newOrder = {
        id: nextOrderId++,
        pizza: selectedPizza,
        status: "ordered"
        }

    orderQueue.push(newOrder)           <<the red line was here


Not in the declared  const newOrder, as 'normaly' happens.



And also the error wasnt very helpfull. So keep in mind this may happen.



The fix was to use the type Order where it was being declared


const newOrder: Order = { id: nextOrderId++....}



--------------------------------


With that problem chimpmonkey stuff about the "string" unlogical thing i stepped on:


If you tried this instead:

if (name === string) {
    // ...
}

JavaScript would look for a variable named string.

Since there isn't one, you'd get:

ReferenceError: string is not defined



SO IT HAS TO BE THIS DUMB SHIT:


if (name === "string") {
    // ...
}


BECAUSE IDFK.


Also i gotta stop forgeting abt this:

strict equality operator:

5 === 5;        // true
5 === "5";      // false
true === 1;     // false






in this code:

return detail.name === identifier


"This is checking if the name property of the current detail object is exactly equal to the identifier value.
If they match, true is returned; if not, false is returned."


so that line (return detail.name === identifier) is going to return the boolean
value of the evaluation.



--



why do i have to write return there before menu.find
I dont get it.

return menu.find(detail =>..)


Because menu.find(...) does find a pizza, but you're not doing anything with the result.



---



My question:
would you say when i use find with an array function or array with objects,
i'll always have to return before like in return menu.find() ?





Answer:




The point is actually not:




"Am I using find()?"




The point is:


"Do I want my function to return the result of find()?"






Case 1: Yes, use return


function getPizzaDetail(id: number) {
    **return** menu.find(pizza => pizza.id === id)                          <<<<<basically find by itself DOESNT RETURN when used in flat logic like that
}

const pizza = getPizzaDetail(2)



getPizzaDetail() is supposed to give the caller a pizza,
so you return the result of find().






Case 2: No, don't use return

Suppose you only want to log the pizza:

function printPizza(id: number) {
    const pizza = menu.find(pizza => pizza.id === id)
    console.log(pizza)
}

printPizza(2)



No return is needed because the function's job is to print
something, not hand a value back.






SO basically i have to watchout if the code inside the function or const is
doing something, or producing a value.




BUT ALSO .find DOESNT AUTOMATICALLY RETURN THE VALUE

SO HOW I KNOW IF A METHOD NEEDS RETURN OR NOT?????????????


answer:


So instead of memorizing EVERY METHOD, you'll ask(Ai probably most times):

Can I assign its result to a variable?

const result = array.find(...)
const filtered = array.filter(...)
const exists = array.includes(...)
const text = name.toUpperCase()

If that makes sense, the method is producing a useful return value.



****
current take:
Hold on what?

isnt the point understanding why find needed return in the first place,
so by those assigns of .find results to the variable mean what really????

figure this out later.........
****

--------------------------------

Function return types


function fetchUserDetails(username: string): User {
    const user = users.find(user => user.username === username)
    if (!user) {
        throw new Error(`User with username ${username} not found`)
    }
    return user
}



by using : User right there in (username: string): User, we can define
what type of data should be returned from our function.



--------------------------------

Void return type Exercise #1


Explicit return types in TypeScript are annotations that define the specific data type a function will return



sometimes we want to explicitly say that a function doesnt return anything
and its just perfoming a operation outside of itself

****
current take:
Maybe this could be of use into my function Section() {}
which is of use only for Object.assign to make the index file and imports.
****


function addNewPizza(pizzaObj: Pizza): void {
    menu.push(pizzaObj)
}


and sometimes we may need to say a function will be a type (Order) but also undefined:

Order | undefined


why?


The reason comes down to the fact that sometimes a function might not be able to return the type you want,
and in this case it would return undefined instead. thats why we may use (Order | undefined)



--------------------------------


TypeScript Fundamentals Add automatic ids to menu items



Go back later to that addNewPizza function crap, i needed to understand the whole context but
brain was burned out.


--------------------------------
***curriculum ***

while refactoring my page for perfomance ive found somethings to remember.

while using Object.assign(SectionType)

if i wanted the function SectionType to actually display what it was returning as parent:

function SectionType() {
    return (
    <main
    className="flex flex-1 w-full max-w-3xl flex-col items-center justify-between py-32 px-16 bg-white dark:bg-black sm:items-start">
    </main>
    )
}

so that it would wrap the other components in it i had to:

Make SectionType accept children



function SectionType({ children }: SectionProps)



and as ive found today, what mostly in typescript always will complain
for a type, which are variable parameters, i had to assign a type (SectionProps)





...Annd at the end of the day it was all for nothing:


So yes...

Right now, this component:

function SectionType({ children }) {
  return <main>{children}</main>;
}

is doing nothing, because it is never called.

It's like writing:

function Hello() {
  console.log("Hello!");
}

function Bye() {
  console.log("Bye!");
}

Hello.Bye = Bye;



it seems the Object.assign, and calling the <Section.ProfileSection/> isnt
doing i thought it was.


I thought that because the function was passed into const Section it sorta
was taking the main with children in it, as i was explicitly using return in there.


My objective with making that function accept children was that previously
i had seen that both cursor and gpt used it with some JSX inside the function
used as parameter for (ProductCard) and (Section) (ProductCard had also type since
the beginning) and i presumed it was somehow wrapping the components, as it was being Used
(mainly in cursor code) as both function and TYPE into the main export function
(const ProductCard) or (const Section) 'in my code' And it seems it had other
purpose than the one i thought.

This may get more easy to understand when i get more hang of Object.assign

--------------------------------
